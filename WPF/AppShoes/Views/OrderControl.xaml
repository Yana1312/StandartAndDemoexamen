<UserControl x:Class="AppShoes.Views.OrderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppShoes.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="BackBorder" BorderBrush="#7FFF00" BorderThickness="1" CornerRadius="20" Margin="5">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel HorizontalAlignment="Left">
                <TextBlock x:Name="ArticulOrderTb" Style="{StaticResource TextBlockStyle}" Text="{Binding OrderId, StringFormat='Артикул заказа: {0}'}"/>
                <TextBlock x:Name="StatusOrderTb" Style="{StaticResource TextBlockStyle}" Text="{Binding OrderStatus, StringFormat='Статус заказа: {0}'}"/>
                <TextBlock x:Name="AdressOrderTb" Style="{StaticResource TextBlockStyle}" Text="{Binding OrderAdressNavigation.Adress, StringFormat='Адрес пункта выдачи: {0}'}"/>
                <TextBlock x:Name="DateCreateOrderTb" Style="{StaticResource TextBlockStyle}" Text="{Binding OrderDate, StringFormat='Дата создания заказа: {0:dd.MM.yyyy}'}"/>
            </StackPanel>

            <TextBlock HorizontalAlignment="Right" Grid.Column="1" x:Name="DateDeliveryOrderTb" Style="{StaticResource TextBlockStyle}" Text="{Binding OrderDeliveryDate, StringFormat='Дата доставки: {0:dd.MM.yyyy}'}"/>
        </Grid>
    </Border>
</UserControl>
